# Server

## Open Ports

When running in development mode, the following ports are open for use during local development:

- `3000`: Default port for the WILDFIRE server.
- `3306`: Port used by MySQL.
- `8080`: Port for accessing the Adminer web interface.

Ensure no other services are occupying these ports to avoid conflicts during development.
