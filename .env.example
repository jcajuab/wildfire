# This file is a template.
# To use it, copy the contents into a `.env` file.
# Make sure to replace all placeholder values with actual, valid values before use.

# --- App ---
PORT=3000

# --- Database ---
MYSQL_ROOT_PASSWORD='A9#fR2!xL7QmZ8p'
MYSQL_HOST='127.0.0.1'
MYSQL_PORT=3306
MYSQL_DATABASE=wildfire_db
MYSQL_USER=wildfire
MYSQL_PASSWORD='R8Qm!Z2p7#Lxf9A'

DATABASE_URL="mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}"

# --- MinIO ---
# MinIO Server Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# MinIO Client Configuration (your app uses these)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_USE_SSL=false
MINIO_BUCKET=content
MINIO_REGION=us-east-1
