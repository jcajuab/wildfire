{
  "name": "wildfire",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "bun run --filter '*' build",
    "check": "biome check --write",
    "dev": "bun run --filter '*' dev"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "lefthook": "^1.12.1"
  },
  "packageManager": "bun@1.2.18"
}
